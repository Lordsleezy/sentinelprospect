import type { Company, Document, Permit, Project, ProjectCompany, Signal, Source } from "./types";

// Synthetic project intelligence is intentionally disabled.
// Local fallback data must come from source-backed collector caches.
export const companies: Company[] = [];
export const projects: Project[] = [];
export const permits: Permit[] = [];
export const projectCompanies: ProjectCompany[] = [];
export const documents: Document[] = [];
export const signals: Signal[] = [];
export const sources: Source[] = [];
